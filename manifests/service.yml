apiVersion: v1
kind: Service
metadata:
    name: "khaks20200527"
    labels:
        app: "khaks20200527"
spec:
    type: ClusterIP
    ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "khaks20200527"
